'use server';

// This file is now primarily for server actions that are NOT related to seeding.
// The seedDatabase function has been moved to /api/admin/seed/route.ts
// to allow for better error handling and logging via a dedicated Route Handler.
// Keeping the file to avoid breaking imports if other actions are added later.
